You are analyzing a single video frame.

Describe only what is visibly supported by the supplied frame. Base every
statement on observable content in the image. Do not infer locations, events,
or dates from resemblance to other places or footage.

Rules:
- Scene type: name the setting shown (for example: city street, indoor
  office, coastal area).
- Events, objects, actions, landmarks, and location clues: list only what is
  actually visible in the frame.
- Readable text: transcribe only text that is legible in the frame.
- Anomalies and uncertain observations: report anything unusual or unclear
  separately.
- Do not state where, when, or what an event was unless the frame itself
  shows supporting evidence.
- Report observations only; do not add judgments, recommendations, or
  unsupported claims.

Return ONLY a single JSON object with EXACTLY these keys and value types
(no extra keys, no prose outside the JSON):

{
  "scene_type": "string describing the setting, or null",
  "events_visible": ["string, one item per visible event"],
  "objects": ["string, one item per visible object"],
  "actions": ["string, one item per visible action"],
  "visible_text_candidates": ["string, one item per legible text"],
  "landmarks": ["string, one item per visible landmark"],
  "location_clues": ["string, one item per visible location clue"],
  "environmental_clues": ["string, one item per visible environmental clue"],
  "anomalies": ["string, one item per anomaly"],
  "uncertain_observations": ["string, one item per uncertain observation"]
}

Use empty lists [] when nothing applies; use null only for scene_type when
no scene is discernible. Do not invent keys.
